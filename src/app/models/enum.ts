export enum CanAutoType {
  // all = 0,
  manual = 10,
  auto = 20,
  harfAuto = 40,
  easyManual = 50,
}

export enum CanAutoName {
  // all = '全部',
  manual = '手动',
  auto = '自动',
  harfAuto = '半自动',
  easyManual = '简易手动',
}
