name: Build assets
on:
  workflow_dispatch:
    inputs:
      TAG:
        description: 'Set a Tag'
        required: true
        default: ''
  push:
    tags:
      - v*
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: set node
        uses: actions/setup-node@v2
        with:
          node-version: '14'
      - name: build dist
        run: |
          echo ${GITHUB_SHA}
          npm install -g @angular/cli
          npm i
          echo start build
          rm -rf statics/static/*
          ng build --configuration production --deploy-url https://cdn.jsdelivr.net/gh/pcrgvg/gvg_front@v${GITHUB_SHA}/statics/static/
          ls statics/static
      - name: commit 
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          branch: master
          add_options: '-A'
          push_options: '--force'
          commit_message: ":bookmark: Release pcrgvg commit sha ${{github.sha}}"
          file_pattern: statics/static/*
      - name: Release to github
        uses: ncipollo/release-action@v1
        with:
            allowUpdates: true
            token: ${{ secrets.GITHUB_TOKEN }}
            tag: v${{github.sha}}